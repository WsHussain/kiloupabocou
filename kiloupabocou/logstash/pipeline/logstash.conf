input {
  beats {
    port => 5044
  }
}

filter {
  if [log][file][path] =~ "nginx" {
    grok {
      match => { "message" => "%{COMBINEDAPACHELOG}" }
    }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "kiloupabocou-logs-%{+YYYY.MM.dd}"
  }
}
